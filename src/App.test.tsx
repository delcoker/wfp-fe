import React from 'react';

test('renders welcome back on login page', () => {
    // render(<App />);
    // const linkElement = screen.getByText(/Welcome Back/i);
    // expect(linkElement).toBeInTheDocument();
});
